<dummy xmlns:if="ant:if" xmlns:unless="ant:unless">
  <param
    name="BOOTSTRAP_TO_TOP_INCLUDE"
    expression="${to-top.include}"
    if:set="to-top.include"
  />
  <param
    name="BOOTSTRAP_CSS_TO_TOP"
    expression="${bootstrap.css.to-top}"
    if:set="bootstrap.css.to-top"
  />
</dummy>
